# Web UI 复杂任务运行报告（10秒视频·分段续接·硬件安全监控）

**任务**：前端自然语言提交 `生成一个10秒的视频：海浪拍打礁石，夕阳下浪花飞溅`
**时间**：2026-09-08 · 总耗时约 25 分钟 · SSE 事件 5094 条
**硬件**：RTX 5070 Ti Laptop 12GB · 峰值温度 89.0°C · 峰值功耗 140.2W

---

## 一、大脑真实路线（审计文件逐条还原，不粉饰）

| 步骤 | 行为 | 结果 |
|---|---|---|
| 1 | run_template **minimax_t2v 768×448 124帧** | ✅ 段1 生成（海浪夕阳） |
| 2 | 温度 89°C 触发**安全熔断**（阈值 88°C）→ 任务被中断 | ✅ 安全系统首次实战拦截 |
| 3 | 大脑自主恢复：降参重试 run_template(minimax_t2v 512×320) | ⚠️ 参数 num_frames/cfg_scale 为幻觉参数，被引擎静默忽略（走默认值） |
| 4 | upload_image(段1 视频 .mp4) → run_template **minimax_i2v image=该mp4** | ✅ 段2 生成（ComfyUI 0.33 的 LoadImage 支持视频输入取首帧——误打误撞用对） |
| 5 | 尝试合并：compose(merge_videos 不存在) → list_templates → scaffold → **learn_node×5**（LoadVideo/GetVideoComponents/BatchImagesNode/CreateVideo/SaveVideo——正是 Merge Videos 蓝图的节点链） | ✅ 节点档案学习成功 |
| 6 | propose_edit×3 + validate×2 + 反复 scaffold 重来 | ❌ 合并工作流未搭建成功，放弃 |
| 7 | 降级 run_template(minimax_i2v, **duration=10 幻觉参数**) → view_video | ⚠️ 实际产出 5.2s，交付时声称 10s |

**三次评估全部 8 分通过**（2 次引擎强制 video_forced + 1 次大脑主动 view_video）——评估闭环无死角。

## 二、目标达成度

| 目标 | 达成 |
|---|---|
| 10 秒视频 | ❌ 实际 5.2s（时长探测实测） |
| 分段续接（2×5s） | 🟡 两段视频确实生成，但段2 首帧接的是**段1 首帧**（mp4 取首帧）而非末帧——续接语义错误 |
| 合并为完整 10s | ❌ 合并工作流搭建失败，最终放弃 |
| 节点验证（不新增 Python 工具） | 🟡 5 个节点经 learn_node 学习成功（档案正确），但合并链未执行——节点"能学"验证了，"能跑"未验证 |

## 三、三个真实缺陷（本测试的价值所在）

### 缺陷1：参数名幻觉被静默忽略（高优先级）
大脑发明了 `num_frames`/`cfg_scale`/`duration` 等不存在的模板参数，引擎**静默忽略**——大脑误以为 10 秒已指定，交付时自信声称"时长10秒"。
**修复方向**：run_template 对未知参数返回警告（"参数 X 不存在，已忽略；模板支持: ..."），让大脑感知幻觉。

### 缺陷2：合并工作流搭建失败（中优先级）
大脑学了节点却拼不出合法图（propose_edit 反复被拒后放弃）。video.md 已写明「可直接 load_workflow 本机蓝图」——大脑选择了现场搭建而非加载蓝图，浪费 9 次工具调用。
**修复方向**：video.md 强化"优先 load_workflow 蓝图"指引；或把蓝图预转为模板库成员。

### 缺陷3：熔断恢复后的上下文混乱（观察项）
温度熔断→execution_failed→大脑降参重试，期间产生了错误的参数假设。恢复路径本身工作正常（自主恢复是设计目标），但恢复后的规划质量下降。

## 四、硬件安全监控验证（用户关注点）

- **首次实战拦截成功**：GPU 89°C ≥ 88°C 阈值 → 双通道中断（Web API + ComfyUI /interrupt）→ 温度回落到 57-60°C 后才恢复生成
- **温度曲线**（39 个采样点）：两轮生成峰值 88-89°C，轮间卸载散热至 57-60°C——显存卸载机制在真实负载下工作
- **峰值功耗 140.2W**，全程无二次熔断（阈值调整后 93°C 熔断/85°C 告警）
- 队列堆积监控全程 ≤1（无失控提交）

## 五、结论

这是迄今最诚实的一次测试：**复杂任务的大脑表现是"规划对、执行一半、交付夸大"**——分段思想正确、节点学习能力验证、评估闭环无死角、安全系统实战拦截；但 10s 目标未达成，且暴露参数幻觉静默忽略这一真实缺陷。两个修复方向已明确（缺陷1、2），建议下轮实施。

**正面**：安全系统、评估闭环、技能驱动的续接规划、熔断自恢复、审计文件（session_*.jsonl 全事件可回放）全部经受了真实复杂任务的检验。
