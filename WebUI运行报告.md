# ComfyUI 大脑 Web UI 运行报告

**测试时间**：2026-09-08
**测试方式**：监控采集器连接 SSE 事件流，顺序执行 5 类不同任务，逐任务抓取全部事件做前后端一致性校验（事件顺序 / 工作流图结构 / 产物落盘 / 评估结果 / 时延），发现问题即时修复并复验。
**环境**：RTX 5070 Ti Laptop 12GB · ComfyUI 0.33.0（秋叶整合包 :8188）· glm-4.5-air（文本）+ glm-4.6v（视觉）· 纯标准库 SSE + 单文件前端

---

## 一、总体结果

| # | 任务类型 | 交付 | 耗时 | 事件数 | 工具调用 | 图校验 | 产物落盘 | VLM评估 |
|---|---|---|---|---|---|---|---|---|
| 1 | 图像单步生成 | ✅ | 21s | 461 | 1 | ✅ 7节点0坏边 | ✅ | — |
| 2 | 失败回流修复（坏工作流） | ✅ | 60s | 1295 | 9（2次失败重试） | 中间态含坏图（预期）→ 终态✅7节点 | ✅ | 通过 |
| 3 | 管线拼接（生成→放大） | ✅ | 168s | 906 | 6（1次失败） | ✅ 15节点0坏边 | ⚠️→✅ 修复后 | 9/10 |
| 4 | 从零自由合成 | ✅ | 75s | 2486 | 16 | ✅ 7节点0坏边 | ✅ | — |
| 5 | 5秒视频+抽帧评估 | ✅ | 414s | 1025 | 1 | ✅ 16节点0坏边 | ✅ | ⚠️ 本轮跳过（见问题2） |

**合计**：5/5 任务交付，总事件 6173，总耗时约 12.3 分钟，零不可恢复失败。

## 二、逐任务明细

### 任务1 · 图像单步生成（21秒）
- 流程：run_template(t2i) → 五段管线 → 交付，1 次工具调用零失败
- 时延：首个思考 delta 0.97s → 首工具 5.97s → 交付 19.43s
- 工作流：7 节点（Checkpoint→CLIP×2→EmptyLatent→KSampler→VAEDecode→Save），边全部有效
- 产物：agent_t2i_00029_.png 已落盘 ✅

### 任务2 · 失败回流修复（60秒）
- 流程：load_workflow 加载损坏文件（引用不存在节点#99）→ run_workflow 返回 **validation_failed 结构化诊断** → 大脑读取后**语义决策重建**（agent_synth 前缀新工作流）→ 执行 → VLM 通过
- 9 次工具调用含 2 次失败重试——失败工具卡片在前端可见（设计预期）
- 图校验中间态 False 为**预期行为**：加载的坏图工作流本就含悬空引用，前端红色高亮依赖此数据

### 任务3 · 管线拼接（168秒）
- 流程：compose(t2i 1024² → upscale_pass 2x) → 15 节点图 → 执行两段 → VLM 9/10
- **发现问题1**（已修复，见第三节）：stage 事件缺失 + 产物清单断链

### 任务4 · 从零自由合成（75秒）
- 16 次工具调用（scaffold → learn_node → propose_edit 循环），思考量 8829 字符为全场最高
- 交付产物经 template_t2i 路径输出——大脑合成验证后选择了模板重跑交付（模型自主决策，属合理但值得关注的路径偏好）

### 任务5 · 5秒视频（414秒 ≈ 7分钟）
- minimax_t2v（768x448、124帧、turbo 8步）真实出片 agent_minimax_00002_.mp4 ✅
- 工作流 16 节点含完整 GUIDER 采样链（CFGGuider→KSamplerSelect→BasicScheduler→RandomNoise→SamplerCustomAdvanced）
- **发现问题2**（见第三节）：本轮大脑跳过 view_video 抽帧评估

## 三、发现的问题与处置

### 问题1：细粒度执行路径的监控断链（已修复 ✅ 并复验）
**现象**：拼接任务走了 submit→wait_result→fetch_outputs 细粒度路径，采集器发现该任务 stage 事件为空、delivery 产物清单为空——前端阶段条与画廊在此路径下不更新。
**根因**：三个细粒度工具未接事件总线，fetch_outputs 不更新 last_outputs。
**修复**：三个工具补齐 stage 事件（submit→running / wait→completed / fetch→completed）并更新产物清单。
**复验**：compose→submit→wait→fetch 全链路重跑，事件序列 `['running','completed','completed']`、last_outputs 更新 2 个文件、53/53 测试通过。

### 问题2：视频评估规则遵循波动（已知，未改代码）
**现象**：本轮大脑执行完视频模板后直接交付，未调用 view_video（上一轮同类任务正确调用了）。
**根因**：glm-4.5-air 对系统提示规则的遵循存在随机波动；评估规则位于提示词中非首位。
**处置**：规则保留并已在映射表中；如需强保证，可在 runner 层对视频产物强制触发评估（列入后续改进）。

### 问题3：采集器空图事件误报（口径修正，非代码缺陷）
**现象**：失败回流任务图校验 False。
**核实**：来自一次空草稿的 workflow_update 事件（前端正确显示占位提示）；坏工作流的中间坏图（含悬空边）恰是前端红色高亮的数据来源，属设计预期。

## 四、前端渲染验证

- **SSE 通道**：11 种事件类型全部实测流经（user_message / memory_recalled / think_delta / tool_start / tool_end / workflow_update / stage / progress / evaluation / delivery / vram）
- **真实浏览器**：Edge 加载页面（标签页"ComfyUI 大脑"）、SSE 长连接建立（netstat ESTABLISHED）、app.js 语法 Node 校验通过
- **事件顺序契约**：5 任务全部满足 user_message 首发 → delivery 收尾
- **工作流图数据**：全部 workflow_update 事件的边引用均可解析（除预期的坏图展示）
- **流式体验**：首个思考 delta 延迟 0.35-1.32s（任务间稳定）

## 五、结论

Web UI 在 5 类异构任务（单步生成/失败修复/管线拼接/自由合成/视频）下前后端数据链路完整，工作流图渲染数据全部结构有效，产物 100% 落盘可溯。监控中发现并修复了细粒度执行路径的监控断链（该缺陷在纯后端测试中不可见，正是本次"跑不同任务+查渲染数据"方法的价值）。遗留改进项：视频评估的强制触发、从零合成后的路径偏好观察。
